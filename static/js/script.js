$(document).ready(function () {
    // Navbar collapse
    $(".sidenav").sidenav({edge: "right"});
    // Task List collapsible
    $('.collapsible').collapsible();
    // Urgent tooltip
    $('.tooltipped').tooltip();
});